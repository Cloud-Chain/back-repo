apiVersion: v1
kind: Service
metadata:
  name: {{ include "contract.fullname" . }}
  labels:
    {{- include "contract.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contract.selectorLabels" . | nindent 4 }}
